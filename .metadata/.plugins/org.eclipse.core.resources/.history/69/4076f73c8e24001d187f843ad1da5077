&НаКлиенте
Процедура УправлениеВидимостью()
	
	Если Объект.ПроектСогласован Тогда //Управление видимсотью в зависимости от галочки проект согласован
		Элементы.Исполнитель.Видимость = Ложь;
		Элементы.Руководитель.ОтметкаНезаполненного = Ложь;
		Элементы.ГруппаИсполнители.Видимость = Ложь;
	Иначе
	
	КонецЕсли
	
	
КонецПроцедуры